<h1>Feed</h1>

<button class="ui primary button">
  Follow
</button>

<% @gifs.each do |gif| %>
  <h3><%= link_to gif.name, gif_path(gif) %></h3>
  <%= link_to(gif) do %>
    <img src="<%= gif.giflink %>" alt="">
  <% end %>
<% end %>
