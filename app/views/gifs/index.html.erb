<h1 class="ui blue header">Feed</h1>

<div class="ui link cards">
  <% @gifs.each do |gif| %>

  <a class="ui card" href="<%= gif_path(gif) %>">
    <div class="content">
      <div class="header"><%= gif.name %></div>
    </div>

    <div class="image">
        <img src="<%= gif.giflink %>">
    </div>
  </a>

  <% end %>
</div>
